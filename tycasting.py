# we need to change the actual data type of the variable
#  here we not concat the string with integer so we do here type casting

num=10
name="Varad"

# so here by giving comma, it will print both values with space because concatination done only with '+'
print(name,num) 

print(name+str(num))